{
	"name": "denon",
	"version": "0.0.1",
	"description": "Denon AVR integration for Node",
	"author": "ChristopherHaws",
	"license": "MIT",
	"private": false,
	"repository": "https://github.com/ChristopherHaws/denon.git",
	"homepage": "https://github.com/ChristopherHaws/denon#readme",
	"bugs": {
		"url": "https://github.com/ChristopherHaws/denon/issues"
	},
	"main": "./lib/main.js",
	"types": "./lib/main.d.ts",
	"scripts": {
		"clean": "rimraf dist",
		"build": "npm run clean && npm run build:ts",
		"build:ts": "tsc",
		"test": "echo \"Error: no test specified\" && exit 1",
		"publish": "npm run clean && npm run build && npm publish --registry https://registry.npmjs.org/"
	},
	"devDependencies": {
		"@types/node": "^14.11.4",
		"copyfiles": "^2.4.0",
		"rimraf": "^3.0.2",
		"ts-node": "^9.0.0",
		"typescript": "^4.0.3"
	},
	"dependencies": {
		"telnet-client": "^1.4.6"
	}
}
